# Quick Start Guide - M5Stack CoreS3 FFT Acoustic Analyzer

## การเริ่มต้นใช้งานอย่างรวดเร็ว

### ขั้นตอนที่ 1: เตรียมอุปกรณ์
- [ ] M5Stack CoreS3
- [ ] สาย USB-C 
- [ ] คอมพิวเตอร์ที่ติดตั้ง Arduino IDE หรือ PlatformIO

### ขั้นตอนที่ 2: ติดตั้งซอฟต์แวร์

#### สำหรับ Arduino IDE:
1. เปิด Arduino IDE
2. ไปที่ `Tools > Manage Libraries`
3. ค้นหาและติดตั้ง:
   - `M5CoreS3` by M5Stack
   - `ArduinoFFT` by Enrique Condes

#### สำหรับ PlatformIO:
1. เปิด PlatformIO
2. สร้างโปรเจคใหม่ด้วยบอร์ด `M5Stack CoreS3`
3. คัดลอกไฟล์ `platformio.ini` ที่ให้มา

### ขั้นตอนที่ 3: อัปโหลดโค้ด
1. เชื่อมต่อ M5Stack CoreS3 กับคอมพิวเตอร์
2. เลือกพอร์ตที่ถูกต้อง
3. อัปโหลดไฟล์ `M5Stack_FFT_SPL_Analyzer.ino`

### ขั้นตอนที่ 4: ทดสอบการทำงาน
1. รีสตาร์ท M5Stack CoreS3
2. คุณจะเห็นหน้าจอ "FFT Spectrum Analyzer"
3. ลองส่งเสียงและดูสเปกตรัมที่เปลี่ยนแปลง

## การใช้งานพื้นฐาน

### ปุ่มควบคุม:
- **ปุ่ม A** (ซ้าย): สลับระหว่างโมด Spectrum และ SPL Meter
- **ปุ่ม B** (กลาง): รีเซ็ตค่าสูงสุด/ต่ำสุด
- **ปุ่ม C** (ขวา): สำรองไว้สำหรับฟีเจอร์เพิ่มเติม

### สิ่งที่จะเห็นบนหน้าจอ:
- **ค่า SPL ปัจจุบัน**: แสดงที่ด้านบน
- **ค่าสูงสุด/ต่ำสุด**: แสดงใต้ค่าปัจจุบัน
- **สเปกตรัม/มิเตอร์**: แสดงตรงกลาง
- **คำสั่งปุ่ม**: แสดงที่ด้านล่าง

## การปรับแต่งพื้นฐาน

### ปรับความไวไมโครโฟน:
แก้ไขไฟล์ `config.h`:
```cpp
#define MIC_SENSITIVITY_DBFS -42.0  // เปลี่ยนค่านี้
```

### ปรับความถี่ sampling:
```cpp
#define SAMPLING_FREQ 44100  // ลดเป็น 22050 หรือ 16000 เพื่อประหยัด CPU
```

### ปรับจำนวนจุด FFT:
```cpp
#define FFT_SAMPLES 1024  // เปลี่ยนเป็น 512 หรือ 2048
```

## การแก้ปัญหาเบื้องต้น

### ไม่มีเสียงจากไมโครโฟน:
1. ตรวจสอบการเชื่อมต่อ USB
2. รีสตาร์ทอุปกรณ์
3. เปิด Serial Monitor เพื่อดู debug message

### ค่า SPL ผิดปกติ:
1. ลองปรับค่า `MIC_SENSITIVITY_DBFS` ใน config.h
2. ทดสอบด้วยแหล่งเสียงที่ทราบค่า SPL

### สเปกตรัมไม่เสถียร:
1. เพิ่มค่า `DISPLAY_UPDATE_INTERVAL` ใน config.h
2. ลดค่า `SAMPLING_FREQ` 

## การขยายความสามารถ

### เพิ่มการบันทึกข้อมูล:
แก้ไขใน config.h:
```cpp
#define ENABLE_SERIAL_LOGGING true
#define DEBUG_MODE true
```

### เปิดใช้ WiFi (อนาคต):
```cpp
#define ENABLE_WIFI true
```

## ความปลอดภัย

⚠️ **คำเตือน**: อย่าใช้ในสิ่งแวดล้อมที่มีเสียงดังเกิน 140 dB

✅ **แนะนำ**: 
- ใช้ในสิ่งแวดล้อมที่มีระดับเสียง 30-120 dB
- ทำการคาลิเบรตก่อนใช้งานจริง
- ตรวจสอบความถูกต้องด้วยเครื่องมือมาตรฐาน

## ความช่วยเหลือเพิ่มเติม

- อ่าน `README.md` สำหรับข้อมูลรายละเอียด
- ดูที่ `config.h` สำหรับการตั้งค่าขั้นสูง
- ตรวจสอบ Serial Monitor เพื่อดู debug information

---

Happy Analyzing! 🔊📊